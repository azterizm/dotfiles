# Ghostty configuration for i3, minimal look, and Gruvbox dark theme.
# Based on your alacritty.toml and your requirements.

# Font
font-family = "JetBrains Mono"
font-size = 14.0

# Padding
window-padding-x = 10
window-padding-y = 10

# Minimal UI
window-decoration = none
gtk-titlebar = false
gtk-tabs-location = hidden
window-subtitle = false

# Colors (Gruvbox dark)
background = #282828
foreground = #ebdbb2

palette = 0=#282828
palette = 1=#cc241d
palette = 2=#98971a
palette = 3=#d79921
palette = 4=#458588
palette = 5=#b16286
palette = 6=#689d6a
palette = 7=#a89984
palette = 8=#928374
palette = 9=#fb4934
palette = 10=#b8bb26
palette = 11=#fabd2f
palette = 12=#83a598
palette = 13=#d3869b
palette = 14=#8ec07c
palette = 15=#ebdbb2

# i3 integration: no maximize/fullscreen, let i3 handle windowing
maximize = false
fullscreen = false

# Remove close confirmation
confirm-close-surface = false

# Remove notifications
app-notifications = false

# Remove window borders and tabs
window-decoration = none
gtk-titlebar = false
gtk-tabs-location = hidden
window-subtitle = false
background = #282828
foreground = #ebdbb2

palette = 0=#282828
palette = 1=#cc241d
palette = 2=#98971a
palette = 3=#d79921
palette = 4=#458588
palette = 5=#b16286
palette = 6=#689d6a
palette = 7=#a89984
palette = 8=#928374
palette = 9=#fb4934
palette = 10=#b8bb26
palette = 11=#fabd2f
palette = 12=#83a598
palette = 13=#d3869b
palette = 14=#8ec07c
palette = 15=#ebdbb2

# i3 integration: no maximize/fullscreen, let i3 handle windowing
maximize = false
fullscreen = false

# Remove title
#title = " "

# Remove close confirmation
confirm-close-surface = false

# Remove notifications
app-notifications = false

# Remove window borders and tabs
# (already set above)
# window-decoration = none
# gtk-titlebar = false
# gtk-tabs-location = hidden

# Remove Adwaita toolbar shadow
adw-toolbar-style = flat

# Remove wide tabs
gtk-wide-tabs = false

# Remove window state saving
window-save-state = never

# Remove scrollbars, overlays, etc.
resize-overlay = never

# Remove split divider color (blend in)
split-divider-color = #282828

# Remove mouse cursor when typing
mouse-hide-while-typing = true

# Remove copy notification
app-notifications = no-clipboard-copy

# Remove system clipboard prompt
clipboard-read = allow
clipboard-write = allow

# Remove copy on select (optional, comment out if you want it)
copy-on-select = true

# Remove bold as bright (optional, comment out if you want it)
bold-is-bright = true

# Remove shell integration title
shell-integration-features = no-title

# Remove window theme
window-theme = ghostty

# Remove Adwaita
gtk-adwaita = false

# Remove desktop notifications
desktop-notifications = false

# Remove quit after last window closed
quit-after-last-window-closed = true

# Remove scrollback limit (optional, set to your preference)
scrollback-limit = 1000000

# Remove selection invert (optional, comment out if you want it)
selection-invert-fg-bg = true

# Remove selection colors (optional, comment out if you want it)
selection-background = #504945
selection-foreground = #ebdbb2

# Remove cursor color (optional, comment out if you want it)
cursor-color = #ebdbb2

# Remove cursor style (optional, comment out if you want it)
cursor-style = block

# Remove blinking cursor (optional, comment out if you want it)
cursor-style-blink = false

# Remove window inherit font size (optional, comment out if you want it)
window-inherit-font-size = false

# Remove window inherit working directory (optional, comment out if you want it)
window-inherit-working-directory = false

# Remove window vsync (optional, comment out if you want it)
window-vsync = false

# Remove window padding color (optional, comment out if you want it)
window-padding-color = background

# Remove window padding balance (optional, comment out if you want it)
window-padding-balance = true

# Remove shell integration (optional, comment out if you want it)
shell-integration = fish

keybind = ctrl+alt+k=goto_split:up
keybind = ctrl+alt+j=goto_split:down
keybind = ctrl+alt+h=goto_split:left
keybind = ctrl+alt+l=goto_split:right

